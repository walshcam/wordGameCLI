//Word Constructor
let Word = function () {
    //An array of "new" letter objects for the letters of the word to be guessed

    //A function that returns a string representing the word. This should call the function on each letter object (the first function defined in 'letter.js') that displays the character or an underscore and concatenated those toegether

    //A Function that takes a character as an argument and calls the guess function on each letter object (the second function defined in letter.js)
}